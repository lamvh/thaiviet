import type { Hero } from '../lib/types';
import content from '../content/site-content.json';

// Editable homepage hero — sourced from site-content.json (managed via /admin).
export const HERO: Hero = content.hero;
